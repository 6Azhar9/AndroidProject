<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a100sewa.a100sewa">

    <Uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignIn" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".Home0"
            android:label="@string/title_activity_home0"
            android:theme="@style/AppTheme" />
        <activity android:name=".list" />
        <activity android:name=".ItemDetalis" />
        <activity android:name=".FreeListing" />
         <meta-data
               android:name="android.support."
               android:value=".MainActivity"
        <activity android:name=".splash"></activity>
    </application>

</manifest>